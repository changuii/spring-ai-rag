services:
  ollama:
    image: 'ollama/ollama:latest'
    ports:
      - '11434:11434'
    volumes:
      - 'ollama-data:/root/.ollama'
  chroma:
    image: 'chromadb/chroma:latest'
    ports:
      - '8000:8000'
    volumes:
      - 'chroma-data:/data'

volumes:
  ollama-data:
  chroma-data:
